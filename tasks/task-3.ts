// Stwórz zmienne:
// 	•	numbers, które będzie tablicą typu number[],
// 	•	userInfo, które będzie krotką zawierającą:
// 	1.	string (imię użytkownika),
// 	2.	number (wiek użytkownika),
// 	3.	boolean (czy użytkownik jest aktywny).

const numbers: number[] = [1, 2, 3, 4, 5];

const userInfo: [string, number, boolean] = ['John', 30, true];
